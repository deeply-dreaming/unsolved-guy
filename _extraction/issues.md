# Extraction issues / notes

This file tracks small extraction quirks and judgment calls made during the current “short & sweet” phase.

## Section B
- `B47`: In `_extraction/guy2004.txt`, the B47 header is not cleanly extracted (the text starts at “Selfridge notices…”). The prompt in `B/B47.md` was derived from that paragraph and the table of contents.
- OCR quirks in `_extraction/guy2004.txt`: Some headings appear as `BID` (B10), `Bll` (B11), `BI5` (B15), `BI6` (B16), `BI9` (B19), `B3S` (B38).

## Section C
- OCR quirks in `_extraction/guy2004.txt`: Some headings appear as `Cl` (C1), `CIO` (C10), `ell` (C11), `CI5` (C15), `CIS` (C18).

## Section D
- OCR quirks in `_extraction/guy2004.txt`: Some headings appear as `Dl` (D1), `DB` (D8), `DID` (D10), `Dll` (D11), `DIS` (D18), `DI5` (D15), `DI9` (D19), `D2l` (D21).

## Section E
- OCR quirks in `_extraction/guy2004.txt`: Some headings appear as `El` (E1), `EIO` (E10), `Ell` (E11), `E1B` (E18).

## Section F
- OCR quirks in `_extraction/guy2004.txt`: Some headings appear as `Fl` (F1), `FlO` (F10), `Fll` (F11), and `FI6`/`FI7`/`FI8`/`FI9` (F16–F19).
